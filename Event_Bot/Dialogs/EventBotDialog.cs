using Microsoft.Bot.Builder.Dialogs;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text.RegularExpressions;
using System.Web;

namespace Event_Bot.Dialogs
{
    public class EventBotDialog
    {
        //public static readonly IDialog<string> dialog = Chain.PostToChin()
        //    .Select(msg => msg.Text)
        //    .Switch(
        //    new RegexCase<IDialog<string>>(new Regex("^hi", RegexOptions.IgnoreCase), (context, text) =>
        //    {
        //        return Chain.ContinueWith(new GreedingDialog(), AfterGreetingContinuationAsync);
        //    })
    }

}